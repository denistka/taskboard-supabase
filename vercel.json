{
  "buildCommand": "cd client && corepack enable && pnpm build",
  "outputDirectory": "client/dist",
  "installCommand": "cd client && corepack enable && pnpm install --frozen-lockfile",
  "framework": "vite"
}

